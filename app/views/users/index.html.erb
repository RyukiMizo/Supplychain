<% provide(:title, '全てのユーザー') %>

<h2>全てのユーザー</h2>

<%= will_paginate %>

<p>ユーザーの検索</p>
<%= form_tag(users_path, method: :get) do %>
  <%= text_field_tag :search %>
  <%= submit_tag '検索', name: nil %>
<% end %>

<ul class="users">
<%= render @users %>
</ul>

<%= will_paginate %>