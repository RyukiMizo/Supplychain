<% if logged_in? %>
  <div class="row">
    <aside class="col-md-4">
      <section class="user_info">
        <%= render 'shared/user_info' %>
      </section>
      <section class="micropost_form">
        <%= render 'shared/micropost_form' %>
      </section>
    </aside>
  </div>
<% else %>
<div class="center jumbotron">
  <h1>全ての商品にはストーリーがある</h1>

  <h2>
私たちは、サプライチェーンを通じて、商品の信頼を提供します。
  </h2>

  <%= link_to "無料で登録", signup_path, class: "btn btn-lg btn-primary" %>
</div>
<% end %>

